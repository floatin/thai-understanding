# PTT 语音语义转录 Pipeline 开发核心方法与步骤

> **目标**：构建一个以语义理解为核心、支持热词扩量和业务上下文注入的端到端语音处理 pipeline，在事中监控与事后分析场景下，通过 SER / Slot F1 / Entity Hit Rate 等指标持续优于现有 Whisper 多模型方案。

---

## 一、核心方法论

### 1.1 设计原则

| 原则 | 说明 |
|---|---|
| **语义优先** | 评估指标以 SER / Slot F1 / Entity Hit Rate 为主，CER 仅作参考 |
| **上下文原生** | 热词、业务场景、对话历史作为 LLM 的 System Prompt 第一公民，而非外挂补丁 |
| **增量迭代** | 数据、热词、场景的扩展通过微调适配器 + 更新 Prompt 完成，不重训 ASR |
| **双轨并行** | 新 pipeline 与现有 Whisper 方案同时运行，以金标准评估集为裁判，逐步切换 |
| **分层评估** | 按错误类型（热词冲突 / 3 模型一致但错 / 难例）分别对比，定位优势子集 |

### 1.2 架构选型

```
音频 ──→ XLSR-Thai（冻参）──→ U-Align 适配器（可训）──→ 语义向量
                                                              │
热词表 + 场景描述 + 对话历史 ──→ System Prompt ──────────────┤
                                                              ▼
                                                    LLM（Qwen3 / 泰语微调 Qwen-ASR）
                                                              │
                                                              ▼
                                                    规范化文本 + 意图 + 槽位
```

**关键决策**：
- 语音编码器：XLSR-Thai（泰语自监督续训，36000h，无 CTC 头）
- 适配器：U-Align 结构（LayerNorm → CNN 下采样 → MLP）
- LLM：自部署 Qwen3 系列或泰语微调 Qwen-ASR（API 无法传自定义向量）
- 对齐目标：InfoNCE + cosine-DTW，Stage 1 冻结文本塔，Stage 2 冻结编码器与 LLM 仅训适配器

---

## 二、开发步骤

### Phase 0：数据资产盘点与标注（第 1 周）

#### 0.1 现有数据确认

| 数据类型 | 量级 | 用途 |
|---|---|---|
| 金标准（人工标注） | 200 条 | 验证集 + 测试集 + 校准锚点 |
| 银标准（3 模型裁判一致） | 5,000 条 | Stage 2 领域微调主力 |
| 铜标准（3 模型裁判不一致） | ~15,000 条 | 难例挖掘候选池 |
| 公开 ASR 数据 | GigaSpeech2 + Common Voice + MSR-86K（几千小时） | Stage 1 对齐 |
| Thai-SUP | 58G（IC/NER/SR parquet + flac） | Stage 2 多任务冷启动 |

#### 0.2 金标准标注 Schema

每条数据标注以下字段（已有则跳过）：

```yaml
audio_id: string              # 音频唯一标识
reference_text: string        # 人工听写文本（泰语原文）
normalized_text: string       # 业务规范化文本
intent: enum                  # 主意图（预订/查询/投诉/闲聊/其他）
entities:                    # 关键实体列表
  - type: string              # 实体类型（hotel/location/date/...）
    value: string             # 实体值
has_hotword_conflict: bool   # 是否含热词冲突
hotword_candidates: list      # 冲突候选（如 ["漫谷","曼谷"]）
correct_hotword: string       # 正确热词
scenario: enum               # 业务场景
audio_duration_sec: float    # 音频时长
```

#### 0.3 金标准分层拆分

```
200 条 ──→ 100 条验证集（永远不动，只算指标）
        ──→ 50 条测试集（最终 A/B 对比）
        ──→ 50 条训练/校准集（可选混入 Stage 2）
```

同时按以下维度标注子集标签（用于后续分层评估）：
- 含热词冲突 / 不含热词冲突
- 3 模型一致 / 不一致
- 短语音（< 3s）/ 长语音
- 有背景噪声 / 无背景噪声

#### 0.4 银标准质量验证

用金标准中"3 模型一致"的子集（约 110 条）计算银标准的真实准确率：
- 若 ≥ 96% → 直接用于 Stage 2
- 若 92–95% → 过滤低置信度样本后使用
- 若 < 92% → 需人工复核或降级为弱监督

---

### Phase 1：基线建立与问题诊断（第 2 周）

#### 1.1 Whisper 方案基线评测

在 100 条验证集上运行现有 3 模型方案（投票 + 热词注入 + LLM 纠偏），记录：

| 指标 | 整体 | 热词冲突子集 | 3 模型一致子集 | 3 模型不一致子集 |
|---|---|---|---|---|
| CER | | | | |
| SER | | | | |
| Slot F1 | | | | |
| Entity Hit Rate | | | | |

#### 1.2 错误分析

- 统计"3 模型一致但错"的样本占比 → 这是新 pipeline 的猎场
- 统计热词冲突子集的错误模式 → 验证"Whisper 热词扩量天花板"假设
- 输出错误分类报告（同音混淆 / 格式差异 / 语义遗漏 / 噪声）

#### 1.3 确定胜负判定标准

> **新 pipeline 胜出的定义**：在 100 条验证集上，SER 不低于 Whisper 方案，且 Entity Hit Rate 显著更高（Δ ≥ 5%）。整体 CER 允许略高（Δ ≤ 2%），不作为否决条件。

---

### Phase 2：Stage 1 语义对齐训练（第 3–5 周）

#### 2.1 数据准备

```
公开 ASR 数据（GigaSpeech2 + Common Voice + MSR-86K）
  → 音频-文本对
  → 统一采样率 16kHz
  → 过滤异常样本（静音/超短/超长）
```

#### 2.2 模型结构

```
XLSR-Thai（Fairseq 权重 → HF 转换，冻参）
    ↓
U-Align 适配器（可训）
    ↓
Qwen3-Embedding-4B 文本塔（冻参，Stage 1 仅对齐）
```

#### 2.3 训练配置

| 参数 | 值 |
|---|---|
| 损失函数 | InfoNCE（句级对比）+ 可选 cosine-DTW（帧级对齐） |
| Batch size | 根据显存调整（单卡 4–6GB 可行） |
| 学习率 | 1e-4（适配器），余弦退火 |
| Epoch | 10–20（监控验证集对比损失） |
| 验证指标 | 音频-文本嵌入余弦相似度 |

#### 2.4 验证

- 随机采样 200 条音频，取 top-5 最近邻文本
- 人工检查语义匹配质量
- 对比损失不再下降 → 进入 Stage 2

---

### Phase 3：Stage 2 领域适配微调（第 6–8 周）

#### 3.1 混合训练数据

```
Thai-SUP（IC/NER/SR 多任务）    70%   ← 通用泰语理解
5000 条银标准（裁判一致）       25%   ← 领域适配
50 条金标准（训练/校准集）      5%    ← 高精度锚点
```

#### 3.2 训练策略

| 阶段 | 冻参部分 | 可训部分 | 数据 |
|---|---|---|---|
| Stage 2a | XLSR-Thai + LLM | 适配器 + LoRA（LLM 浅层） | Thai-SUP + 银标准 |
| Stage 2b | XLSR-Thai | 适配器 + LLM LoRA | + 金标准校准集 |

#### 3.3 多任务目标

```
IC（意图分类）    → Cross-Entropy
NER（实体识别）   → BIO Tagging Loss
SR（语义转录）    → CTC / AR Decoding Loss
对齐（对比）      → InfoNCE（保留 Stage 1 能力）
```

#### 3.4 热词 Prompt 注入

```
System Prompt 模板：
  你是一个泰语语音语义转录系统。
  当前场景：{scenario}
  可用热词列表：{hotword_list}
  对话历史：{history}
  请输出：规范化文本 + 意图 + 实体列表
```

---

### Phase 4：A/B 评测与迭代（第 9–10 周）

#### 4.1 评测执行

在 100 条验证集上并行运行：

```
方案 A：现有 Whisper 3 模型方案（投票 + 热词 + LLM 纠偏）
方案 B：新 pipeline（XLSR-Thai → 适配器 → LLM + 热词 prompt）
```

#### 4.2 分层指标对比

| 子集 | 方案 A SER | 方案 B SER | 方案 A Entity Hit | 方案 B Entity Hit | 结论 |
|---|---|---|---|---|---|
| 整体 | | | | | |
| 热词冲突 | | | | | |
| 3 模型一致但错 | | | | | |
| 3 模型不一致 | | | | | |
| 短语音 | | | | | |

#### 4.3 决策树

```
新 pipeline SER ≥ Whisper 且 Entity Hit Rate 高 ≥ 5%
  → ✅ 新 pipeline 胜出，准备灰度上线

新 pipeline SER 略低（Δ < 2%）但 Entity Hit 显著高
  → ⚠️ 考虑路由式部署（见 Phase 5）

新 pipeline SER 明显低（Δ ≥ 2%）
  → ❌ 继续迭代（加数据 / 调架构 / 难例精标）
```

---

### Phase 5：路由式部署（可选，第 11–12 周）

#### 5.1 架构

```
PTT 音频 ──→ 3 模型并行推理
                  │
         3 模型一致？ ──→ 是 ──→ 直接输出（低延迟）
                  │
                  否
                  ▼
         新 pipeline（XLSR-Thai → 适配器 → LLM + 热词 prompt）
                  │
                  ▼
         输出规范化文本 + 意图 + 槽位
```

#### 5.2 收益

| 维度 | 效果 |
|---|---|
| 整体 SER | 高于纯 Whisper 方案（难例被新 pipeline 消化） |
| 热词冲突 SER | 显著优于纯 Whisper |
| 推理延迟 | 一致样本走快速通道，仅难例走 LLM |
| 迭代成本 | 新 pipeline 独立优化，不影响 Whisper 基线 |

---

### Phase 6：持续优化（长期）

#### 6.1 数据飞轮

```
线上推理 → 3 模型不一致样本 → 人工标注 → 加入金标准
                                     → 加入 Stage 2 训练集
                                     → 更新热词表
```

目标：每季度新增 200–500 条金标准，持续扩充训练集。

#### 6.2 热词扩量

- 从 50 个 → 200 个 → 500 个
- 每轮扩量后跑 Entity Hit Rate 对比
- Whisper 方案在 > 100 个热词时预期性能下降，新 pipeline 应保持稳定

#### 6.3 Prompt 迭代

- 新增业务场景 → 更新 System Prompt 中的场景描述
- 新增对话状态 → 更新历史上下文注入
- 无需重训模型

#### 6.4 架构升级路径

| 阶段 | 升级方向 |
|---|---|
| 当前 | XLSR-Thai + U-Align + Qwen3 |
| 中期 | 替换为 Qwen3-ASR 式端到端语音-语言融合 |
| 远期 | 原生多模态（如 Qwen3-Omni 开源泰语版） |

---

## 三、关键风险与应对

| 风险 | 概率 | 影响 | 应对 |
|---|---|---|---|
| Stage 1 对齐不充分 | 中 | 高 | 增加公开数据量 / 延长训练 / 检查权重转换 |
| 银标准噪声导致 Stage 2 过拟合 | 中 | 中 | 严格质量验证 / 降低银标准比例 / 加入更多公开数据 |
| LLM 自部署推理延迟过高 | 高 | 中 | 路由式部署 / 模型量化 / 小模型（3B）+ 蒸馏 |
| 冷启动 SER 未打平 Whisper | 中 | 低 | 路由式部署兜底 / 继续迭代不急于全量切换 |
| 热词 prompt 过长导致 LLM 注意力分散 | 低 | 中 | 热词检索（只注入相关热词子集）/ 分层 prompt |

---

## 四、里程碑与交付物

| 里程碑 | 时间 | 交付物 |
|---|---|---|
| M1：数据就绪 | 第 1 周 | 标注 schema 完成、金标准分层、银标准质量报告 |
| M2：基线报告 | 第 2 周 | Whisper 方案分层指标 + 错误分析报告 |
| M3：Stage 1 对齐完成 | 第 5 周 | 适配器权重 + 对齐验证报告 |
| M4：Stage 2 微调完成 | 第 8 周 | 领域适配模型 + 初步评测 |
| M5：A/B 评测报告 | 第 10 周 | 分层对比报告 + 决策建议 |
| M6：路由部署上线 | 第 12 周 | 双轨系统运行 + 监控面板 |

---

## 五、一句话总结

> **用 SER 衡量业务价值，用热词扩量验证架构优势，用金标准分层评估定位胜负子集。新 pipeline 不是"替代"Whisper，而是"接管它做不到的事"——热词无限扩量、业务上下文原生支持、增量迭代不重训。**
